# Agent Rules

## Git Workflow
- Whenever you create or modify any `.html` files (or other project source files) within a git repository, always automatically stage, commit, and push the changes before finishing your turn.
- Use:
  1. `git add <modified-files>`
  2. `git commit -m "<descriptive, concise commit message>"`
  3. `git push origin <current-branch>`
